<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.acorn.StoreMapper">

	<select id="test1" resultType="StoreDTO">
		SELECT *
		FROM store_tbl
	</select>

	<select id="test2" resultType="StoreDTO"
		parameterType="String">
		SELECT *
		FROM store_tbl
		where storeCode = #{storeCode}
	</select>

<!--  	<insert id="sellerHome" parameterType="com.example.Store">
		INSERT INTO store_tbl 
		(storeName, storeCategory, storeImage, storeAddress, storePhone, minOrderPrice, deliveryFee, deliveryArea)
		VALUES 
		(#{storeName}, #{storeCategory}, #{storeImage}, #{storeAddress}, #{storePhone}, #{minOrderPrice}, #{deliveryFee}, #{deliveryArea})
	</insert> -->
</mapper>