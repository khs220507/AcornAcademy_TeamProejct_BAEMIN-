<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.acorn.SellerMapper">
	<!-- 매장정보 1개 조회
	<select id="selectStoreSubInfo" resultType="StoreDTO" parameterType="String">
		SELECT storeRating, reviewCount, minOrderPrice, storeDescription, operatingTime, storeAddress FROM where storeCode=#{storeCode}
	</select> -->
	<!-- 매장정보 수정 
	<update id="update" parameterType="Member">
		UPDATE phonetbl
		SET brand = #{brand} ,
		since = #{since)
		WHERE modelname = #{modelNm}
	</update> -->
</mapper>